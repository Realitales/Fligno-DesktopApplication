if xampp cannot import the db

then create a database within your browser localhost phpmyadmin with the name of syscommdb and follow the column and its value

table name = syscommtable

firstname varchar(255)
lastname varchar(255)
fullname varchar(255)
department varchar(255)
salary decimal(10,2)
contact varchar(255)
password varchar(255) PRIMARY KEY
datehired date 